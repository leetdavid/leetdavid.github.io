export interface PCSpecs {
  case: string;
  storage: string[];
  motherboard: string;
  cpu: string;
  gpu: string;
  cooler: string;
  ram: string[];
  mouse: string;
  keyboard: string;
  monitor: string[];
}
